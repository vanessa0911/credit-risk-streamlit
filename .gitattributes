# Normalisation des fins de ligne (recommandé)
* text=auto

# --- Modèles & artefacts lourds (toujours en LFS) ---
*.joblib filter=lfs diff=lfs merge=lfs -text
*.npy    filter=lfs diff=lfs merge=lfs -text

# --- Logs CatBoost (ciblés par dossier) ---
artifacts/catboost/*.tsv      filter=lfs diff=lfs merge=lfs -text
artifacts/catboost/*.tfevents filter=lfs diff=lfs merge=lfs -text
artifacts/catboost/*.json     filter=lfs diff=lfs merge=lfs -text

# --- IMPORTANT ---
# Pas de règle globale pour *.csv (on veut pouvoir versionner
# artifacts/local_example.csv SANS LFS).

# Si un jour vous voulez mettre certains CSV précis en LFS,
# décommentez et adaptez les lignes ci-dessous :
# application_train_clean.csv filter=lfs diff=lfs merge=lfs -text
# application_test_clean.csv  filter=lfs diff=lfs merge=lfs -text
